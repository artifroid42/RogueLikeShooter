using UnityEngine;

public class FixLaPositionDeCeBatardDeMerde : MonoBehaviour
{
    // Update is called once per frame
    void LateUpdate()
    {
        transform.localPosition = Vector3.zero;
    }
}
